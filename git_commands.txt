git config

git init

git add file.

git commit -m  "COMMENT"

git status

git log

git diff ==> version controls

git checkout ==> turning back

githup => without readme u can add via git and link

Follow the github 

git remote add origin "LINK"

git push -u origin master

	origin is the root master is the branch here



