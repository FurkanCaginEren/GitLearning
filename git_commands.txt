git config

git init

git add file.

git commit -m  "COMMENT"

git status

git log

git diff ==> version controls

git checkout ==> turning back

githup => without readme u can add via git and link

Follow the github 

git remote add origin "LINK"

git push -u origin master

	origin is the root master is the branch here


Master Branch is followed by main program in localy 

Remote Reposityory can be parelel or sinked together

Local rep is ==> .git

Remote rep is ==> GitHub

Derine yardım edildi

git branch name_of_branch ==>  used branch_merge 

git checkout branch-merge ==> changing branch in git







